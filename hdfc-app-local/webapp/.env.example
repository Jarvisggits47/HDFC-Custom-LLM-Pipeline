# Optional overrides — the app works with zero configuration.
# Uncomment to change the default local model, embedding model, or where
# the SQLite DB and retriever/adapter artifacts are stored.
# LOCAL_MODEL=Qwen/Qwen2.5-1.5B-Instruct
# EMBED_MODEL=BAAI/bge-small-en-v1.5
# MIN_RETRIEVAL_SCORE=0.35
# PIPELINE_DB_PATH=./pipeline.db
# RETRIEVER_DIR=./runs
